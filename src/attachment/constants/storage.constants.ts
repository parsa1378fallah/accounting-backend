import { join } from 'path';

export const STORAGE_CONSTANTS = {

    ROOT_DIRECTORY:
        process.cwd(),

    STORAGE_DIRECTORY:
        'storage',

    ATTACHMENTS_DIRECTORY:
        'attachments',

    TEMP_DIRECTORY:
        'temp',

};